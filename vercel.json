{
  "buildCommand": "prisma db push --accept-data-loss && prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
